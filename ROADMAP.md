# OpenBake Frontend Roadmap

> 기준일: 2026-08-25
> 대상: `openbake-fe` — Next.js 16 App Router, React 19, TypeScript 5, Tailwind CSS 4, TanStack Query 5

## 1. 목표

OpenBake 프론트엔드를 데모 수준에서 실제 사용자 흐름을 안전하게 수행할 수 있는 상태로 발전시킨다.

- 구매자가 일반상품과 드롭 상품을 탐색하고 구매할 수 있다.
- 판매자가 상품·드롭·주문·정산을 관리할 수 있다.
- 관리자의 승인·정산 작업에는 오조작 방지 장치가 있다.
- 모든 주요 실패 상태에서 사용자가 재시도하거나 안전하게 이탈할 수 있다.
- 변경할 때마다 lint와 production build를 통과한다.

## 2. 작업 원칙

1. 기본적으로 프론트 저장소만 수정한다. 백엔드 변경이 꼭 필요하면(예: 게스트 브라우징을 위한 보안 정책 완화) 반드시 사용자의 명시적 승인을 받고 최소 범위로만 수정하며, 기존 테스트 패턴을 그대로 따라 테스트도 같이 추가한다. 승인 없이 코드로 우회하지 말고 `docs/backend-api-requests.md`에 기록한다.
2. 작업 전 관련 API 문서와 실제 백엔드 응답(가능하면 실제 Controller/DTO/Service 소스)을 함께 확인한다. 문서와 실측이 다르면 실측 결과를 우선한다.
3. 한 작업은 한 가지 사용자 문제만 해결하도록 작게 나눈다.
4. 새로운 UI 라이브러리를 일괄 도입하지 않는다. 필요한 shadcn/ui 컴포넌트만 개별 추가한다.
5. API 상태는 TanStack Query, 인증 토큰과 찜처럼 서버 API가 없는 상태만 브라우저 저장소로 관리한다.
6. 완료 표시는 코드 작성뿐 아니라 lint, build, 주요 사용자 흐름 확인까지 끝난 뒤 갱신한다.

## 3. 현재 상태

### 완료된 기반

- [x] Next.js App Router, TypeScript, Tailwind CSS 기반 구성
- [x] 공통 테마와 모바일 중심 레이아웃
- [x] 로그인(이메일/구글)·회원가입·토큰 재발급·로그아웃
- [x] API Gateway 단일 진입점 전환(`NEXT_PUBLIC_API_BASE_URL`, 개별 서비스 포트 직접 호출 안 함)
- [x] 상품·드롭·카테고리·검색·추천 화면
- [x] 게스트(비로그인) 상품/드롭 상세 조회, 목록·자동완성 API(홈/카테고리/검색 하부)
- [x] 일반상품 판매자 등록·수정 화면
- [x] 일반상품 다중 장바구니(담기/수량변경/픽업일변경/삭제)
- [x] 드롭 구매(입장 확정 → 재고 선점 → 주문서 생성 → 새로고침 복구 → 결제/명시적 취소)
- [x] 일반상품 장바구니 → 주문서 생성 → 결제(카트 체크아웃)
- [x] 바로구매(장바구니 없이 상품 상세에서 즉시 주문서 생성)
- [x] 주문 목록·상세·취소(신규 계약 — `items[]` 배열, 항목 단위 구매확정)
- [x] 판매자 항목별 구매확정
- [x] 예치금 충전(Toss) 및 거래내역 화면
- [x] 판매자 신청·승인·드롭 CRUD·정산 화면
- [x] 관리자 판매자 승인·정산 화면
- [x] `/order`, `/wallet`, `/seller` 레이아웃 인증 가드

### 현재 핵심 제약

- 완전한 게스트 브라우징은 부분적이다 — 상품/드롭 **상세**와 홈/카테고리/검색이 쓰는 목록·자동완성 API는 게스트에게 열려 있지만, **추천(recommendations)은 개인화가 본질이라 의도적으로 로그인 필수로 남아있음**(게스트 화면에서는 추천 섹션 자체를 숨김).
- 로그인 유도 시 `returnTo`(원래 보던 화면으로 복귀) 패턴이 `/wallet/charge`에만 있고 `/login` 자체엔 없음 — 401로 튕기거나 로그인 버튼을 누르면 항상 홈으로 감.
- 인증 가드(`useAuth()` + `useEffect` 리다이렉트)가 `(shop)`/`admin`/`order`/`wallet`/`seller` 5곳에 동일 패턴으로 중복돼 있고 공용 훅으로 추출되지 않음.
- 판매자 상품/드롭 "수정" 화면은 단건 조회 API가 없어 목록에서 find로 우회 중(백엔드 제약, 규모가 커지면 비효율).
- 일부 `/internal/v1/*` API의 서버 권한 검사가 부족하다. 관리자 화면의 프론트 가드는 보조 수단일 뿐 근본 보안 대책이 아니다.
- 자동화 테스트 러너가 아직 없다.
- 위시리스트(찜)는 백엔드 API 자체가 없어 `localStorage` 전용(기기 간 동기화 안 됨). 드롭 참여 이력(`GET /drops/history`)도 백엔드에 없음.

## 4. 실행 로드맵

### R0. 기준선 확립

- [x] `CLAUDE.md`의 완료 마일스톤과 현재 아키텍처(게이트웨이 단일 진입점) 갱신
- [x] Node.js 및 패키지 매니저 권장 버전 명시
- [x] `.env.example` 작성 및 실제 비밀값이 없는지 점검
- [x] `npm run lint` 통과
- [x] `npm run build` 통과
- [ ] 주요 라우트 수동 스모크 테스트 표 작성(개별 플로우 확인은 여러 차례 있었으나 정리된 표는 아직 없음)
- [ ] 백엔드 API 문서(`docs/*-api.md`)와 현재 실제 응답 차이 재점검

### R1. 인증·라우트 보호 — P0

목표: 비로그인 또는 권한 없는 사용자가 보호 화면에서 오류 상태에 빠지지 않게 한다.

- [ ] 공용 `AuthGuard` 추출(현재 5개 레이아웃에 동일 패턴이 중복돼 있음)
- [x] `/seller`, `/order`, `/wallet` 레이아웃에 인증 가드 적용
- [ ] 로그인 이동 시 원래 경로 보존 및 로그인 후 복귀(`returnTo` 패턴을 `/login`까지 확장)
- [x] 관리자 역할을 로그인 응답의 실제 `role` 값에서 파생(임의 값 아님)
- [ ] 인증 실패 시 불필요한 Query 재시도 중단
- [x] 판매자 전용 화면의 승인 상태 가드(각 페이지가 `getMySeller()`로 확인)

완료 기준:

- 비로그인 상태로 보호 URL에 직접 접근하면 로그인 화면으로 이동 ✅
- 로그인 후 최초 요청 URL로 복귀 — 미완료
- 일반 회원은 판매자·관리자 기능을 실행할 수 없음 ✅

### R2. 구매 흐름 — 대부분 완료, P0

목표: 드롭과 일반상품 흐름을 각각 명확히 분리하고 실패해도 복구 가능하게 한다.

#### 드롭 구매

- [x] 입장 확정(`confirm-entry`) 실패 코드별 안내(`DR006` 이미 참여, `DR008` 판매 전/마감/품절)
- [x] 드롭 상태(`dropStatus`) 자동 폴링 — 카운트다운 종료 시 드롭 쿼리 갱신
- [x] 주문 화면 진입 시 서버 주문 상세(`getOrderDetail`) 재조회, URL엔 orderId만
- [x] 서버 금액·수량·픽업일을 화면의 최종 기준으로 사용(URL 파라미터로 안 실음)
- [x] 주문서 유효시간(`reservationExpiresAt`) 카운트다운 및 만료 상태 표시
- [x] 결제 페이지 이탈 후 재진입(`OR006`→`GET /orders/pending` 확인) 흐름
- [x] 입장 확정 → 재고 선점 → 주문서 → 결제/명시적 취소 종단 간 확인

#### 일반상품 구매

- [x] 상품 추가·수량 변경·픽업일 변경·삭제
- [x] 장바구니 체크아웃(`POST /orders {cartItemIds}`) 연동
- [x] 바로구매(`POST /orders {productId,quantity,pickUpDate}`) 연동
- [ ] 장바구니 항목 선택(체크박스) 없이 주문 가능한 항목 전체를 자동 포함 — 부분 선택 UI는 아직 없음

완료 기준:

- 어느 실패 경로에도 무한 로딩이나 행동 불가능 상태가 없음 ✅
- URL 파라미터 조작으로 화면 금액과 실제 주문 금액이 달라지지 않음 ✅
- 드롭과 일반상품이 같은 주문 화면(`/order`)을 안전하게 공유함 ✅

### R3. 금전·관리자 안전장치 — P0

목표: 주문 취소, 판매자 승인, 정산 지급의 오조작과 중복 요청을 방지한다.

- [ ] 공용 `ConfirmDialog` 구현(현재 `window.confirm` 사용 중)
- [ ] 주문 취소 전 주문번호·상품·환불 예정 금액 확인
- [ ] 판매자 승인·반려 전 대상 사업자 확인
- [ ] 반려 사유 필수화
- [ ] 정산 지급 전 판매자·기간·금액·정산 ID 확인
- [ ] 지급 멱등키를 재시도에도 유지하도록 수정
- [ ] 지급 실패 사유와 외부 거래 ID 검증
- [ ] 백엔드 관리자 권한 검사(`/internal/v1/*`) 완료 전 배포 차단 항목으로 관리

완료 기준:

- 모든 비가역 작업이 대상 정보를 보여주는 확인 절차를 거침
- 같은 지급 요청을 재시도해도 중복 지급되지 않음
- 프론트 권한 조작만으로 관리자 API를 실행할 수 없음

### R4. 판매자 운영 흐름 — P1

목표: 판매자가 신청 이후 상품 운영과 주문 처리까지 완료할 수 있게 한다.

- [ ] 판매자 신청 폼을 sessionStorage에 임시 보존
- [ ] 이전 단계 이동 및 최종 계좌정보 확인 화면 추가
- [x] 반려 사유와 다음 행동 안내(대시보드에 상태 배지 + 반려 사유 표시)
- [ ] 백엔드 지원 후 재신청 기능 연결
- [ ] 대시보드에 진행 드롭 판매율·다음 드롭·최근 정산 요약 추가
- [x] 판매자 주문 목록 API 연동(`GET /sellers/me/orders`, 항목 단위 구매확정)
- [x] 픽업 완료/구매확정 화면(항목별 구매확정 버튼 + 상태 표시)
- [ ] 상품·드롭 삭제 후 관련 목록 캐시 무효화 검증

완료 기준:

- 판매자는 신청, 승인, 상품 등록, 주문 확인, 픽업 확정, 정산 조회 흐름을 수행 가능
- 새로고침 때문에 판매자 신청 정보가 예기치 않게 사라지지 않음

### R5. 공통 UX·오류 처리 — P1

목표: 화면마다 다른 로딩·오류·빈 상태를 일관된 패턴으로 정리한다.

- [ ] QueryClient 공통 `retry`, `staleTime`, 오류 정책 설정
- [ ] 공용 `LoadingState`, `EmptyState`, `ErrorState` 구현
- [ ] API 오류 코드별 사용자 메시지 매핑
- [ ] 홈·검색·추천·찜·주문 목록에 재시도 버튼 제공
- [ ] 충전 금액 최소·최대·단위 검증
- [ ] 이미지 로드 실패 폴백과 업로드 미리보기
- [ ] 삭제된 찜 항목 정리
- [ ] 24시간 이상 남은 드롭의 날짜형 카운트다운 표시

완료 기준:

- 네트워크 실패가 빈 화면으로 표시되지 않음
- 오류 메시지에 사용자가 수행할 다음 행동이 포함됨

### R6. 접근성·반응형 — P2

목표: 키보드, 스크린리더, 모바일 환경에서 주요 흐름을 사용할 수 있게 한다.

- [ ] 모든 입력에 연결된 `label`과 오류 설명 제공
- [ ] 동적 오류에 `role="alert"` 또는 `aria-live` 적용
- [ ] 탭바에 `nav`, 활성 링크에 `aria-current` 적용
- [ ] `focus-visible` 스타일 통일
- [ ] 주요 터치 영역 최소 44px 확보
- [ ] 색상 대비와 SOLD OUT 배지 가독성 점검
- [ ] 320px, 375px, 768px, 데스크톱 레이아웃 점검
- [ ] 키보드만으로 로그인부터 주문 직전까지 수행

완료 기준:

- 주요 흐름에 키보드 포커스 단절이 없음
- 입력 오류와 비동기 상태 변경을 스크린리더가 인식함

### R7. 테스트·배포 준비 — P2

목표: 반복 개발 중 핵심 사용자 흐름의 회귀를 자동으로 탐지한다.

- [ ] Vitest와 React Testing Library 도입 검토 및 설정
- [ ] API 클라이언트·포맷터·상태 매핑 단위 테스트
- [ ] Playwright 도입 및 핵심 E2E 시나리오 작성
- [ ] PR 기준 lint, build, test 자동화
- [ ] 운영 환경변수와 허용 Origin 목록 확정
- [ ] 에러 추적 및 Web Vitals 수집 방식 결정
- [ ] Next.js 이미지 도메인과 보안 헤더 점검
- [ ] 배포 전 체크리스트 작성

우선 E2E 시나리오:

1. 회원가입 → 로그인 → 로그아웃
2. 드롭 상세 진입 → 입장 확정 → 재고 선점 → 주문서 → 결제/취소
3. 일반상품 탐색 → 장바구니 추가·변경·삭제 → 체크아웃 → 결제
4. 바로구매 → 결제
5. 판매자 신청 → 관리자 승인 → 상품/드롭 등록·수정·삭제
6. 판매자 항목 구매확정 → 정산 조회 → 지급 확인 절차

완료 기준:

- 핵심 흐름이 CI에서 자동 검증됨
- 운영 배포에 필요한 환경변수와 보안 설정이 문서화됨

## 5. 백엔드 선행 작업

| 항목 | 막고 있는 프론트 기능 | 우선순위 |
|---|---|---:|
| `/internal/v1/*` 관리자 권한 검사 | 안전한 정산 배포 | P0 |
| 판매자 재신청 API | 반려 후 재신청 | P1 |
| 판매자 상품/드롭 단건 조회 API | 수정 화면 효율 | P2 |
| 위시리스트 서버 API | 기기 간 찜 동기화 | P2 |
| 드롭 참여 이력(`GET /drops/history`) | "내가 참여한 드롭" 화면 | P2 |

백엔드 대기 항목은 임시 mock 데이터로 완료 처리하지 않는다. 사용자에게 "준비 중" 상태를 보여주고, API 계약이 확정되면 연동한다.

## 6. Claude Code 작업 방식

각 작업은 아래 순서로 진행한다.

1. `CLAUDE.md`, `ROADMAP.md`와 관련 API 문서를 읽는다.
2. 관련 파일과 호출 경로를 조사한다(문서보다 실제 백엔드 소스를 우선).
3. 수정 전에 구현 계획과 영향 파일을 제시한다.
4. 한 번에 로드맵 체크박스 1~3개만 구현한다.
5. 기존 동작을 보존하고 범위 밖 리팩터링은 하지 않는다.
6. `npm run lint`와 `npm run build`를 실행한다.
7. 변경 파일, 동작 변화, 검증 결과, 남은 위험을 보고한다.
8. 검증이 완료된 항목만 `[x]`로 변경한다.

## 7. 작업 완료 보고 형식

```markdown
## 작업 결과
- 완료한 ROADMAP 항목:
- 변경 파일:
- 사용자 관점의 변화:
- API 계약 또는 백엔드 의존성:

## 검증
- npm run lint:
- npm run build:
- 수동 확인:

## 남은 위험
-

## 다음 권장 작업
-
```

## 8. 다음 작업

R1/R2의 핵심 사용자 플로우는 대부분 끝났다. 다음 우선순위는 **R3 금전·관리자 안전장치**(P0) — 특히 공용 `ConfirmDialog`와 정산 지급 멱등키 유지부터 시작하는 것을 권장한다. R1의 남은 항목(`returnTo` 로그인 복귀, 공용 `AuthGuard` 추출)도 작지만 여전히 P0로 분류돼 있으니 다음 세션 초반에 같이 처리할 수 있다.
